# Week 6 Day 1 协作日志

## 日期
2026-04-25

## 今天完成的任务
1. Clone Flasky 项目到本地
2. 修复配置问题（SERVER_NAME 环境变量）
3. 初始化数据库（运行 flask deploy）
4. 解决端口冲突（macOS AirPlay 占用 5000 端口，改用 5001）
5. 创建测试用户（跳过邮箱验证）
6. 启动 Flask 开发服务器
7. 浏览器测试核心功能（登录、发文章、评论、个人资料）

## 遇到的问题和解决方案

### 问题 1: psycopg2 安装失败
- **现象**: pip install 时报错，缺少 pg_config
- **原因**: 默认 requirements.txt 指向 Heroku 配置，需要 PostgreSQL
- **解决**: 使用 requirements/dev.txt，开发环境用 SQLite

### 问题 2: 配置文件报错
- **现象**: 启动时 KeyError: 'SERVER_NAME'
- **原因**: ProductionConfig 强制要求 SERVER_NAME 环境变量
- **解决**: 修改 config.py，将 `os.environ['SERVER_NAME']` 改为 `os.environ.get('SERVER_NAME')`

### 问题 3: 端口 5000 被占用
- **现象**: curl 返回 403 Forbidden，Server 显示 AirTunes
- **原因**: macOS AirPlay 服务占用 5000 端口
- **解决**: 改用 5001 端口启动 Flask

### 问题 4: 注册功能无法使用
- **现象**: 注册时提示"邮箱无效"
- **原因**: Flasky 需要发送确认邮件，但邮件服务未配置
- **解决**: 创建 Python 脚本直接在数据库创建测试用户，设置 confirmed=True 跳过验证

## 测试用户信息
- 用户名: testuser
- 邮箱: test@example.com
- 密码: password123
- 已确认: True

## Flasky 核心功能（用户视角）

### 游客（未登录）
- 浏览文章列表
- 阅读文章详情
- 查看评论

### 注册用户
- 注册/登录/登出
- 发布新文章
- 编辑/删除自己的文章
- 评论文章
- 关注/取消关注用户
- 查看和编辑个人资料

### 管理员
- 管理所有用户
- 删除任何文章和评论
- 管理用户权限

## 今天最有效的 prompt
```
我刚 clone 了 flasky。我不读代码。
1. 给我"如何在本地跑起来"的命令清单
2. 跑起来后，列我应该测试的 5 个用户操作
3. 每个操作"成功的标志"是什么（业务话）
```
**为什么有效**: 明确了"不读代码"的边界，要求业务化的验证标准，CC 给出的步骤清晰可执行。

## 今天我对 CC 输出的怀疑
- **时间**: 14:30
- **CC 说**: 使用默认 requirements.txt 安装依赖
- **我跑出来**: psycopg2 安装失败
- **我用什么验证的**: 查看 requirements/ 目录，发现有 dev.txt，改用开发环境依赖成功

## 今天踩的坑
1. **端口冲突**: macOS 的 AirPlay 占用 5000 端口，浏览器无法访问
2. **邮箱验证**: 注册功能需要真实邮件服务，开发环境需要手动创建测试用户

## 今天我学到的业务概念
- **Flask 应用配置分层**: 开发/测试/生产环境使用不同配置，开发环境用 SQLite 更轻量
- **用户确认流程**: 注册后需要邮箱确认，可以通过设置 confirmed=True 跳过

## 今天我没看代码做的验证有几次
3 次：
1. 浏览器测试登录功能（输入用户名密码，看是否跳转到首页）
2. 浏览器测试发文章功能（填写标题内容，看是否出现在列表）
3. 浏览器测试评论功能（添加评论，看是否显示在文章下方）

## 明天计划
1. 完成业务功能图（如果需要）
2. 开始标签系统设计
3. 实现标签存储功能
